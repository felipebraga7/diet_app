import 'package:flutter/material.dart';

class AppColorsLightTheme {
  const AppColorsLightTheme._();

  static const primary = Color(0xff2162d5);
  static const onPrimary = Colors.white;
  static const secondary = Color(0xFF979797);
  static const surface = Colors.white;
  static const inverseSurface = Colors.black;
  static const onSecondary = Colors.blueGrey;

  static const success = Color(0xff17B963);
  static const successGradientStart = Color.fromRGBO(23, 185, 99, 0.3);
  static const successGradientEnd = Color.fromRGBO(23, 185, 99, 0);

  static const info = Color(0xff2162d5);
  static const infoGradientStart = Color.fromRGBO(52, 127, 248, 0.3);
  static const infoGradientEnd = Color.fromRGBO(52, 127, 248, 0.0);

  static const warning = Color(0xffFCD031);
  static const warningGradientStart = Color.fromRGBO(252, 208, 49, 0.3);
  static const warningGradientEnd = Color.fromRGBO(252, 208, 49, 0.0);

  static const error = Color(0xffEC4830);
  static const errorGradientStart = Color.fromRGBO(236, 72, 48, 0.3);
  static const errorGradientEnd = Color.fromRGBO(236, 72, 48, 0.0);

}
